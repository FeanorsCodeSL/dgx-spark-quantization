# Nemotron Plan Moved

The canonical Nemotron run plan now lives with the run:

[`runs/nemotron-3-nano-omni-30b-a3b/PLAN.md`](../../runs/nemotron-3-nano-omni-30b-a3b/PLAN.md)

Keeping the active plan under `runs/<slug>/` matches the existing Qwen run
layout and avoids two divergent copies of the same model-specific plan.

# Core quantization stack
torch>=2.5
transformers>=4.46
safetensors>=0.4.5
accelerate>=1.0
psutil>=5.9
compressed-tensors>=0.7.1
llmcompressor>=0.9.0  # AWQ + compressed-tensors W4A16 (Nemotron-Omni Phase 2b)

# Hugging Face ecosystem (Hub + CLI + accelerated transfer)
huggingface_hub[cli,hf_transfer]>=0.25

# Evaluation harness
lm-eval==0.4.11

# Optional but useful
pyyaml>=6.0
tqdm>=4.66
